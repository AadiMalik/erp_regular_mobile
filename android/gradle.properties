org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Djava.net.preferIPv4Stack=true
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Incremental Kotlin compilation repeatedly corrupts its .tab cache files on
# this machine (file-locking race, likely AV real-time scanning) - disabling
# it trades a slightly slower full recompile for a build that actually finishes.
kotlin.incremental=false
